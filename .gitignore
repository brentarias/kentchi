# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# temporary credential handoff files (never commit)
Cloudinary_access.md

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# playwright mcp artifacts
.playwright-mcp/

# scratch pad: unused / superseded art kept only as a local frame of reference
# (OG card experiments, favicon variants, monogram source + traces, design
# screenshots). Never served by the site; never committed.
scratch_pad/

# dev / debug screenshots (manual page captures, not website content)
screenshots/

# art pipeline: print-quality masters and processed derivatives
# (everything here is sensitive or a build artifact; never commit)
art-pipeline/

# video assets — staged here for now, will move to Contentful when wired.
# Files are dropped manually for staging deploys; posters and source-of-truth
# live in the POD provider / Kentchi's personal archive (and art-pipeline/masters/).
public/videos/
